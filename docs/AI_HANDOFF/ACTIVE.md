# ACTIVE

Cycle: P — permission detail + builtin tool-call UI + VSIX release pass

## State
done

## Notes
- 3/3 approved; full suite 838 passed / 2 skipped exit 0; compile+typecheck clean; vsdb-1.6.0.vsix built (not published).
