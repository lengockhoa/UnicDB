# PLAN — Handoff Plan (currently empty)

This file is the active handoff plan. Each cycle overwrites the previous plan
when shipped (the prior plan is archived under `archive/cycle-<name>-plan.md`).

No active cycle — see `INDEX.md` for the in-flight task queue and `HISTORY.md`
for completed cycles.
