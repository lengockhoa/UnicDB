Command: handoff-fullstack
Goal: Implement the full AI Chat V2 replacement (17 coder-ready tasks, TASK-CHATV2-001..017)
Base: main
Phase: R2 (final review round)
Cursor: Fix round 1 PASS and independently re-verified: commits 18efae2/4eb97de/57223e5/e34c8d3; gates compile 0, typecheck 0, V2E2e 20/20, webview/aiChat 393/393, full suite 4637 pass/5 skip/0 fail, forbidden-token PASS, tree clean. R2 adversarial re-review IN FLIGHT (code-reviewer, independent model): verifying the 3 P1 closures (twin-suppression gate races, ready fan-out ordering, permission settle/optionId invertibility) + new-P1 hunt + non-chat-webview regression sweep. R1 verdict CRITICAL (docs/AI_HANDOFF/REVIEW-CHATV2-R1.md): P1-1 dual-render duplicate messages (host dual-posts legacy delta + V2 text_delta, aiChatPanel.ts ~2990/3235/3450/4233/3924); P1-2 model chip dead at boot (V2 models frame only sent as set_model ack ~2578, ready ~2370 posts legacy-only); P1-3 double permission surface (legacy card ~3655 + V2 sheet; stale sheet wedges composer Enter, raw-ACP optionIds disable Allow). Security gates PASS. Coverage hole: no dual-wire bundle test (4624 green tests never drove both wire families). FIX ROUND 1: P1 fixes ALL committed — 18efae2 (P1-1 single-renderer seam + bundle dual-wire E2E + re-pinned V2 AG-001 sweep), 4eb97de (P1-2 V2 models frame on ready handshake), 57223e5 (P1-3 raw-ACP allow-kind optionIds normalize onto V2 sheet, single surface + settle contracts). P2 tail in flight: describe-husk removal (aiChatPanelWebview.test.ts uncommitted); AG-001 sweep re-pinned; dual-wire E2E test landed in 18efae2.
Next: when fix round 1 reports — re-run gates (npm run compile, typecheck, aiChatPanelV2E2e, webview/aiChat, full npm test, forbidden-token), commit, then spawn R2 code-reviewer re-review (focus: 3 P1s closed, no new P1; round cap = 2). If APPROVED/APPROVED-WITH-MINOR → final wrap: INDEX done, docs/STATUS.md + WORKLOG via update-status, RUN.md Phase done, Final Report (explicitly note NO version bump/package/publish this cycle; task #10 publish remains user's call). If open P1s → round 2 fixes (final). Do NOT re-plan. Do NOT version-bump/package/publish.
