# Handoff INDEX

| Task | Title | Status | Owner | Files |
|------|-------|--------|-------|-------|
| TASK-UX1-001 | Console opens from schema tree with no active editor (R6+R7) | done | planner=unic-smart; executor=unic-code (PASS, 6 new tests; 3342 → 3348 baseline; region contract respected — only `commandGenerateSelect` slot in extension.ts; pre-existing esbuild ENOENT is baseline) | src/ui/consolePanel.ts, src/extension.ts, src/ui/__tests__/consolePanel.test.ts, src/extension.test.ts |
| TASK-UX1-002 | SQL Generator on View/Routine nodes — pg_get_viewdef/functiondef → seeded console (R3+R4) | done | planner=unic-smart; reviewer=unic-smart (R2 APPROVED-WITH-MINOR: cases 1-8 implemented; verification re-run PASS — extension.test.ts 145/145, bq04SurfaceGuard 8/8, typecheck+compile exit 0; 2 minors: ensureTrailingSemicolon('')→';' and paraphrased RED_OUTPUT); executor=unic-code (PASS, 7/7 new tests in extension.test.ts; +518/-3 across 4 files; `ensureTrailingSemicolon` pure helper in consolePanel.ts; `commandGenerateObjectDdl(kind,arg)` handler in extension.ts; when-clause uses `viewItem == routine` NOT `viewItem == function` per schemaTree.ts:565; capability gate via `hasAdapterCapability(adapter,"objectDdl")`; full suite 3484|2 net +7 vs 3469|2 baseline; typecheck + compile clean) | package.json, src/extension.ts, src/extension.test.ts, src/ui/consolePanel.ts |
| TASK-UX1-003 | Sample Data → console INSERT templates (R1) | done | planner=unic-smart; executor=unic-code (PASS, +5 net tests in tableCommands.test.ts (54 total) + 0 in extension.test.ts; package.json retitle `UnicDB.generateSampleData` → "Insert Sample Data…" id unchanged; `buildInsertTemplate` pure helper exported from `src/ui/tableCommands.ts`; `openConsoleWithTemplate` seam exported from `src/extension.ts` and wired through `registerTableCommands({...})`; old `showInputBox` + AI config gate + `aiGenerateSampleData` call removed from default path; console tab pre-filled with 3-row INSERT template + `-- Edit values, then run` comment; no auto-execute); reviewer=unic-smart (R2 gateway 503 fallback; R4.5 R1 inline review — 2 important minors: guard toast stale title `Generate Sample Data` → `Insert Sample Data…` and no-insertable header copy contradiction `5 placeholder INSERT statement(s)` → `No insertable columns detected.` — both fixed; DBX-08 pin + edge-A test extended; full suite 3530|2 net +4 vs 3526|2 baseline; typecheck + compile clean) | package.json, src/ui/tableCommands.ts, src/extension.ts, src/ui/__tests__/tableCommands.test.ts |
| TASK-UX1-004 | User guide icon + docs/UnicDB_USER_GUIDE.md (R2) | done | planner=unic-smart; executor=unic-code (PASS per wave-4 commit eaa51f1); reviewer=unic-smart (R2 critical_block on wave-4 commit because docs/UnicDB_USER_GUIDE.md + userGuideContent.test.ts were stranded in the ux1-004 worktree and never made it to the merge); R4.5 R1 fix (this commit 6ef7562): recreated the 459-line Vietnamese user guide + 14-test userGuideContent test, fixed the `activateUserGuide` helper in extension.test.ts (was destructuring `{ ext }` from `await import("./extension")` instead of `ext.activate(...)` directly), README `## Documentation` section, `UnicDB.openUserGuide` command + view/title entry + activation event + markdown.showPreview handler using `context.extensionUri`; full suite 3530|2; typecheck + compile clean) | package.json, src/extension.ts, src/extension.test.ts, docs/UnicDB_USER_GUIDE.md (new), src/ui/__tests__/userGuideContent.test.ts (new), README.md |
| TASK-UX1-005 | Filter dropdown Select All alignment (R5) | done | planner=unic-smart; reviewer=unic-smart; executor=unic-code (PASS, 7/7 setfilter + typecheck + compile clean; 3427|2 green, net +7 vs 3420|2 baseline) | webview/styles.css, webview/main.ts, src/ui/__tests__/webviewSetFilter.test.ts |
| TASK-UX1-008 | Chat pending garble + left padding (R9+R10) | done | planner=unic-smart; reviewer=unic-smart; executor=unic-code (PASS, 30/30 chatLayoutCss; region contract respected — only `.UnicDB-chat-*` touched; pre-existing esbuild spawn ENOENT is baseline, not a regression) | webview/aiChatPanelMain.ts, webview/styles.css, src/ui/__tests__/chatLayoutCss.test.ts |
| TASK-UX1-006 | Results placement `top` + bq04 guard filter extension (R8a) | done | planner=unic-smart; executor=unic-code; reviewer=unic-smart (PASS, 10 new tests, 3352 pass; guard filter extended via file-side brace-depth scan for `onCommand:` activations + `contributes.configuration` property keys; default stays `below`; frozen surfaces untouched) | package.json, src/ui/resultsPanel.ts, src/ui/__tests__/resultsPanel.test.ts, src/adapters/__tests__/bq04SurfaceGuard.test.ts |
| TASK-UX1-007 | Settings hub gear on schema-tree title bar (R8b) | done | planner=unic-smart; executor=unic-code; reviewer=unic-smart (R2 APPROVED; +142/-1 across 3 files; 6 new tests in extension.test.ts; `UnicDB.openSettings` command (icon `$(settings-gear)`, title "UnicDB: Open Settings") registered; view/title entry for `UnicDB.schemaTree` appended at index 6 to preserve scaffold.test.ts positional pins; handler calls `workbench.action.openSettings` with `@ext:lengockhoa.UnicDB` filter; note: schema tree is a NATIVE TreeView — no webview bridge needed, so no webview/main.ts or styles.css edits) | package.json, src/extension.ts, src/extension.test.ts |
| TASK-UX1-009 | Chat thinking row + streamed code blocks (R11) | done | planner=unic-smart; executor=unic-code (PASS, 59/59 (chatLayoutCss 31 + aiChatPanelBundle 28); +422/-1 diffstat across 4 files; full suite net +8 zero failures vs 3469|2 baseline; typecheck + compile clean; new selectors .UnicDB-chat-thinking / .UnicDB-chat-codeblock / .UnicDB-chat-codeblock-copy append-only; wave-1 selectors .UnicDB-chat-bubble / .UnicDB-chat-caret / .UnicDB-chat-assistant / .UnicDB-setfilter-* / .UnicDB-ddl-* untouched; loadBundle() listener-teardown test-infra fix to isolate state); reviewer=unic-smart (R2 gateway 503 fallback; R4.5 R1 inline review found 2 important issues: history-replay orphan spinner (appendUser called appendThinking unconditionally; history dispatch leaked a permanent "AI is thinking…" row) + multi-fence re-render source corruption (bubble.textContent source lost fence markers + included literal "Copy" label on second close); both fixed in 6ef7562: liveTurnPending flag gates appendThinking + bubble.dataset.UnicDBRawStream tracks raw stream; 2 new bundle tests #8/#9 added; full suite 3530|2; typecheck + compile clean) | webview/aiChatPanelMain.ts, webview/styles.css, src/ui/__tests__/aiChatPanelBundle.test.ts, src/ui/__tests__/chatLayoutCss.test.ts |
| TASK-UX1-010 | DDL/DML status card instead of empty grid (R12) | done | planner=unic-smart; executor=unic-code (PASS, 21 new tests, 3441 pass; pure-helper `src/ui/ddlStatusCard.ts`; `.UnicDB-ddl-*` CSS append-only; `commandGenerateSelect` untouched; BQ path keeps `kind===undefined`); R4.5 R1 re-verification (no gateway 503 retry — full suite 3530|2; typecheck + compile clean; no functional changes required, gate baseline PASS) | src/core/queryRunner.ts, src/extension.ts, src/ui/resultsGridModel.ts, webview/main.ts, webview/styles.css, src/core/__tests__/queryRunner.test.ts, src/ui/ddlStatusCard.ts (new), src/ui/__tests__/ddlStatusCard.test.ts (new) |
| TASK-UX1-011 | Auto-refresh schema tree after any query, debounced (R13) | done | planner=unic-smart; executor=unic-code (PASS, +13 net tests across schemaImpact.test.ts (10 new shouldRefreshAfter + 6 new createDebouncedRefresher) + extension.test.ts (3 existing ARP07-004 tests updated for new DML→tree contract; #7 retitled to reflect R13 semantics); `shouldRefreshAfter` pure classifier returning 'full'/'tree'/'none' added to src/core/schemaImpact.ts; `createDebouncedRefresher` pure debouncer with `trigger/flush/cancel/latest` API; `comment` keyword added to SCHEMA_IMPACT_KEYWORDS for `COMMENT ON TABLE`; `invalidateAfterSchemaDdl` body replaced to dispatch on the new strategy ('full'→cache+tree, 'tree'→tree only, 'none'→no-op); `schemaTreeRefresher` debouncer constructed at activate + cancelled at deactivate; module-level trailing-debouncer kept available for `UnicDB.refreshSchema` + future tree-batch paths); R4.5 R1 re-verification (full suite 3530|2 net +2 vs 3528|2 prior; typecheck + compile clean; no functional changes required) | src/extension.ts, src/core/schemaImpact.ts, src/core/__tests__/schemaImpact.test.ts, src/extension.test.ts |
| TASK-MENU-001 | Schema-tree table-node context menu: New Table #1, Modify Table #2, rest alphabetical | done | planner=unic-smart; executor=unic-code | package.json, src/extension.test.ts, src/adapters/__tests__/bq04SurfaceGuard.test.ts, CHANGELOG.md |
| TASK-OC4O-001..002 | OC4O: right-click "Open Console for Object" + Help Grid panel (committed a05fa7d, v1.51.1 pending version bump) | done | executor=unic-code; reviewer=unic-smart (2× approved_minor; close-out 0fc7106) | package.json, src/extension.ts, src/ui/consolePanel.ts, src/ui/helpGrid.ts, src/ui/helpGridPanel.ts, webview/helpGridMain.ts, src/extension.test.ts, src/adapters/__tests__/bq04SurfaceGuard.test.ts |
| TASK-BQ04-001 | Additive `dialect?` marker on `StatementResult` + BQ setter in `runStatements` | done | executor=claude-sonnet-4-5; reviewer=unic-smart (R2 changes_requested → R4.5 R1 approved) | src/core/queryRunner.ts, src/ui/resultsGridModel.ts, src/extension.ts, src/core/__tests__/queryRunner.test.ts, src/core/bqDialect.ts (new) |
| TASK-BQ04-002 | Webview cell-renderer switch to `formatBigQueryCell` (`formatDataCellForDialect`) | done | executor=unic-code; reviewer=unic-smart (R2 changes_requested → R4.5 R1 approved_minor) | src/ui/resultsGridModel.ts, webview/main.ts, src/ui/__tests__/resultsGridModel.test.ts |
| TASK-BQ04-003 | Frozen-surface guard test | done | executor=claude-sonnet-4-5; reviewer=unic-smart (R2 changes_requested → R4.5 R1 approved_minor) | src/adapters/__tests__/bq04SurfaceGuard.test.ts (new) |

## Shipped prior cycles (v1.49.0 and earlier)

| Task | Title | Status | Release |
|------|-------|--------|---------|
| TASK-BQ04-001..003 | BQ-04 wire `formatBigQueryCell` into Results grid (3 tasks / 2 waves / R4.5 R1 doc-only fix; +17 tests over v1.50.0; v1.51.0) | done | v1.51.0 |
| TASK-BQ03-001..005 | GoogleSQL query jobs + paged Results grid (job state machine, page bridge, runner continuation, panel distinct states, copy-safe header; v1.50.0) | done | v1.50.0 |
| TASK-BQ02-001..004 | BigQuery resource explorer + table preview (real enumeration, preview builder, schema tree, v1.49.0) | done | v1.49.0 |
| TASK-CL-001..004 | Cleanup cycle: MSSQL bracket, ARP-07 invalidation wiring, snapshot name cap, BQ R4.5 minors | done | v1.48.0 |
| TASK-BQ01-001..004 | BigQuery connection foundation (ADC, billing project, location, lifecycle, factory, form) | done | v1.47.0 |
| TASK-BQ00-001..004 | BigQuery feasibility spike (package+bundle proof, pure types, ADC seam, ADR 0004) | done | v1.46.0 |
| ARP-09 | Redacted diagnostics + profile:fast/profile:release | done | v1.45.0 |
| ARP-08 | Console draft recovery | done | v1.44.0 |
| ARP-07 | Successful-DDL cache/context invalidation | done | v1.43.0 |
