# PLAN — Cycle 2026-08-23-H: carry-over hardening + release v1.5.1

Cycle: 2026-08-23-H · Base: main @ 7959bee · Planner: orchestrator (unic-smart)

## §1 Intent

Cycle G closed, v1.5.0 released. "Continue" → run the part that needs no user input: clean up carry-over minors from G reviews (rev606 + rev604). User already provided enough context earlier; no open questions.

## §2 Scope (3 tasks, 1 wave)

Carry-over hardening — NO new features:

1. **TASK-701** — `EXPLAIN [ANALYZE] DELETE/TRUNCATE/DROP/UPDATE...` currently classifies `other/none` → guard modal is skipped, even though ANALYZE actually executes the real statement. Fix: skip-past-EXPLAIN prelude in `analyzeStatement`.
2. **TASK-702** — `capDetail` slices by UTF-16 code unit → can cut in the middle of a surrogate pair / variation selector (detail modal shows corrupted characters). Fix: slice by code point.
3. **TASK-703** — release hygiene: package-lock.json root version stale (1.3.0 vs package.json 1.5.0); add a test pinning versions in sync (package.json ↔ package-lock root ↔ README mentions the latest version).

## §3 Approach

- **701**: in `analyzeStatement` (src/core/dangerousStatement.ts), after masking literals, detect a statement starting with `explain` (the first depth-0 word, case-insensitive) → optional `analyze`/`analyse`/`verbose`/`analyze verbose`/costs... → skip to the real statement keyword. Careful: `EXPLAIN (ANALYZE, COSTS) DELETE FROM t;` — the parenthesized option list is at depth>0 (already skipped by the depth check), so we only need to skip `explain` + optional modifier words at depth 0, then let the STATEMENT_STARTERS logic keep running. But existing logic: the first depth-0 keyword decides the kind — `explain` is NOT in DML_KINDS → `other` and `break` immediately. Fix = do NOT break on `explain` (when followed by `analyze|analyse|verbose`), keep scanning until the real keyword. CTE `WITH` after EXPLAIN must also work (`EXPLAIN ANALYZE WITH c AS (...) DELETE FROM t`) → treat as a prelude similar to `with`: set the kind by the first STATEMENT_STARTERS encountered after the prelude. Simplest: when `explain` appears at depth 0 (and no other keyword has been seen yet), set `sawExplain = true` and continue; if afterwards `analyze|analyse|verbose|costs|buffers|timing|summary|format` appears at depth 0 → continue as well (options inside parens are already skipped by the depth check; these words appear outside parens in the form `EXPLAIN ANALYZE DELETE`). When STATEMENT_STARTERS fires → kind is decided. `hasWhere` is already a regex over the full masked text — unchanged.
- **702**: `capDetail` — replace `joined.slice(0, cap)` with slice-at-codepoint-boundary: if `charCodeAt(cap)` is a high surrogate (0xD800-0xDBFF) → cut at cap-1. (Variation selector U+FE0F is its own BMP code unit — cannot be split by the surrogate regex check; the real concern is surrogate-pair emoji. Enough for the fix.)
- **703**: `npm install --package-lock-only` to sync lock root version → 1.5.0. New test `src/__tests__/releaseHygiene.test.ts`: read package.json + package-lock.json + README.md, assert: lock root version === package.json version; README contains the current version string (badge / "vsdb-<version>.vsix" pattern — README uses placeholder `<version>`, so assert differently: README either mentions the exact latest version in the updates section if present; if only placeholder → assert the placeholder exists + note). Careful not to make the test fail on a version bump — the test MUST read the version from package.json dynamically, NOT hardcode.

## §4 Test Plan (TDD)

- **701** (src/core/__tests__/dangerousStatement.test.ts — append):
  - `EXPLAIN DELETE FROM t` → kind=delete, tier red (no WHERE)
  - `EXPLAIN ANALYZE DELETE FROM t` → red
  - `EXPLAIN (ANALYZE, COSTS) UPDATE t SET a=1` (no WHERE) → red
  - `EXPLAIN ANALYZE SELECT * FROM t` → none (select not dangerous)
  - `EXPLAIN ANALYZE WITH c AS (SELECT 1) DELETE FROM t WHERE x=1` → kind=delete, hasWhere=true → amber
  - `EXPLAIN ANALYZE UPDATE t SET a=1 WHERE id=2` → none (update has where)
  - Regression: `EXPLAIN SELECT 1` still other/none; ordinary DELETE without EXPLAIN still red.
- **702** (each task needs its own test file because capDetail is private in extension.ts — do NOT export production code just for tests. Put the logic in src/core/text.ts (new): `export function truncateAtBoundary(s: string, cap: number): string`; extension.ts imports and uses it. Test src/core/__tests__/text.test.ts: ASCII unchanged; string with emoji at the cap boundary → does NOT cut in the middle of a surrogate pair (result ends on a valid character, Array.from(s).length ≤ expected); strings shorter than cap are unchanged.
- **703** (src/__tests__/releaseHygiene.test.ts — new): lock root === pkg version; README mention pattern exists (placeholder `vsdb-<version>.vsix` or dynamic version string).
- RED first, GREEN after — every executor must paste the real RED output.

## §5 Verification Commands

- Targeted: `npm run compile && npx vitest run src/core/__tests__/dangerousStatement.test.ts src/core/__tests__/text.test.ts src/__tests__/releaseHygiene.test.ts`
- Typecheck: `npm run typecheck`
- Wave boundary: `npm run compile && npx vitest run` (full 38+ files)

## §6 Acceptance Criteria

- [ ] EXPLAIN ANALYZE DELETE (no WHERE) now triggers red modal; SELECT/INSERT after EXPLAIN still none
- [ ] capDetail does NOT produce corrupted characters when text contains an emoji at the boundary
- [ ] package-lock root version = 1.5.0; hygiene test green
- [ ] Full suite green (≥443 tests); typecheck 0
- [ ] Every task has real RED/GREEN output in the Executor Report
- [ ] Reviewer (different model than executor) re-runs verification + verdict

## Planner Self-Audit

- Overlapping files? 701 (core/dangerousStatement + test), 702 (core/text.ts new + extension.ts 1 import line / 1 use line + test), 703 (package-lock + new test). Spread out — 1 wave is safe. extension.ts only touched by 702.
- Discriminating tests? 701: current code fails RED for real (explain → other). 702: current capDetail cuts surrogate → RED. 703: lock 1.3.0 ≠ 1.5.0 → RED.
- Low risk: does NOT touch webview/AG Grid; 701 is a pure function; 702 extracts a helper without changing ASCII behavior.

## Outcome

4/4 approved (701/702/704 approved_minor; 703 approved). 702 needed 1 auto-fix round (RED_OUTPUT paste). Gates: 40 files / 453 tests, tsc 0, vsix 1576198 bytes. Released: tag v1.5.1 + GitHub release asset verified. Close commit 0438762.
