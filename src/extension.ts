// src/extension.ts — VSDB extension entry stub.
// TASK-001 scaffold: keep signature stable; TASK-007 will replace body with real wiring.
import * as vscode from "vscode";

export function activate(context: vscode.ExtensionContext): void {
  // Stub: TASK-007 wires commands, schema tree, CodeLens, status bar.
  void context;
}

export function deactivate(): void {
  // Stub: TASK-007 disposes resources on shutdown.
}
